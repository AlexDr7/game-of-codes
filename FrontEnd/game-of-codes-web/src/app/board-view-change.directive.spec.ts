import { BoardViewChangeDirective } from './board-view-change.directive';

describe('BoardViewChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardViewChangeDirective();
    expect(directive).toBeTruthy();
  });
});
